name: bandu_wrong_notebook
description: Flutter client for the Bandu wrong-notebook app.
publish_to: none

version: 0.2.0+1002

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+2
  dio: ^5.5.0+1
  flutter_riverpod: ^2.5.1
  flutter_secure_storage: ^9.2.2
  go_router: ^14.2.0
  image_picker: ^1.1.2
  path: ^1.9.0
  path_provider: ^2.1.4
  file_selector: ^1.1.0
  speech_to_text: ^7.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
