# `:feature:tags` 最小任务列表

模块职责：九学科标准标签树和自定义标签管理。

依赖模块：`:domain`、`:core:model`、`:core:designsystem`。

需求：`PRD-TAG-*`。

## 任务

- [x] `TAGS-001` 创建 Feature 模块和标签页 Contract。验证：模块编译。
- [x] `TAGS-002` 实现九学科切换和标准标签树展开/收起。验证：树 UI 测试。
- [x] `TAGS-003` 标准标签显示层级、编码和关联错题数量。验证：Compose 测试。
- [x] `TAGS-004` 禁止标准标签改名或删除。验证：操作入口不存在。
- [x] `TAGS-005` 实现自定义标签创建和重名错误展示。验证：ViewModel 测试。
- [x] `TAGS-006` 实现自定义标签改名。验证：成功和冲突测试。
- [x] `TAGS-007` 删除自定义标签前显示关联错题数，删除后不删除错题。验证：交互测试。
- [x] `TAGS-008` 点击标签发出预置筛选的错题列表导航意图。验证：导航测试。
- [x] `TAGS-009` 添加空状态、大树和 200% 字体测试。验证：Compose 测试。

## 模块完成条件

- [x] `:feature:tags:testDebugUnitTest` 通过。
- [x] 标准标签只读，自定义标签行为符合 `PRD-TAG-*`。
