# next-bbs 克隆路径与命令

## 克隆路径

- **路径**：`e:\workspace\maker_world\next-bbs`（与 maker-world-clone 同级目录）

## 克隆命令

```bash
git clone git@git.lasercyber.com:fullstack/next-bbs.git "e:\workspace\maker_world\next-bbs"
```

## 访问说明

- 需要能访问 `git.lasercyber.com` 并具备该仓库的 SSH 或读写权限。
- 若在其它机器或 CI 中执行，请将路径替换为当前环境下的约定路径（如组织规范中的工作区根或 CI 工作目录）。
